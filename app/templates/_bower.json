{
  "name": "<%= _.slugify(appname) %>",
  "private": true,
  "version": "0.0.0",
  "dependencies": {<% if (includeBootstrap) { if (includeCompass) { %>
    "sass-bootstrap": "~3.0.0",<% } else { %>
    "bootstrap": "~3.0.3",<% }} %>
    "jquery": "~1.10.2"
  },
  "devDependencies": {}
}

